## Render the README ----
## tar_mermaid doesn't semem to be working as advertised rn.
# merm <- targets::tar_mermaid(targets_only = TRUE, outdated = FALSE,
#                              legend = FALSE, color = FALSE)
# readr::write_lines(merm, here::here("merm.txt"))
# quarto::quarto_render(here::here("README.qmd"))
